import { InlineBanner } from "../layout/InlineBanner";

export const UpdateNotice = () => (
  <InlineBanner>
    Quiet Team is intentionally version-one simple. Use this surface for lightweight updates later.
  </InlineBanner>
);
