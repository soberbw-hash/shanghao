import { InlineBanner } from "../layout/InlineBanner";

export const RecordingConsentNotice = () => (
  <InlineBanner tone="warning">
    Recording stays manual-only. Make sure everyone in the room knows when it is on.
  </InlineBanner>
);
