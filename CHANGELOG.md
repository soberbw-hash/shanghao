# Changelog

上号只记录正式对外版本。当前正式支持平台为 Windows 10/11 x64。

## 2.9.0 - 2026-08-15

### 房间场景与角色

- 新增只显示今日日期的简洁挂历、动态天气窗、系统定位与全国城市检索；天气、日历和时钟统一房间视觉。
- 空闲工位使用轻量场景屏幕；整理角色入场、离场和换座动画，修复角色移动不可见、标签闪烁以及状态消息让角色偶发弹回旧座位。
- 悬浮窗控制等待缩短到 1 秒，继续支持纵向拖动、屏幕边界、网格吸附和恢复位置。
- KK 系游戏统一显示“KK 对战平台”，不再暴露具体 hosted 游戏名称或图标。

### 语音与好友响度

- 扬声器菜单新增默认开启的好友响度匹配，以约 -14 LUFS 为目标，并保留逐好友音量、本地静音和峰值保护。
- DeepFilter 处理加入环境噪声、回声、近端说话和双讲状态，根据 VAD 与远端参考调整抑制并保护句首、轻声和双讲。
- 五人 Mesh、ICE restart、TURN、按成员语音兜底、后加入成员音频和屏幕分享媒体路径保持兼容。

### 录音库与本地语音记忆

- 录音库支持多选与批量删除；“清理录音”检查短录音、近似静音和无法读取的废录音，收藏及带 Marker 的文件受到保护。
- 本地语音记忆接入录音库，补充转录进度、失败原因、从头重转、说话人确认、搜索和问答数据链路。
- 新增转录质量门禁，拒绝乱码、状态标签、无可靠人声和异常重复文本；AI 模型、Runtime、结果和检查点继续保存在本机。
- 语音房、屏幕分享、Peer 恢复或内存压力较高时降低 AI 下载和推理占用，优先保护实时语音。

### 日常交互与工程维护

- 替换高频按钮点击音效，快捷消息“上号”使用独立语音；“问”入口统一为简洁名称。
- 屏幕分享能力检查使用真实 Electron/Windows 捕获能力，修复错误提示和临时捕获源失败后的重试。
- RoomClient 继续作为统一入口，内部拆分 Presence、成员事件、社交、Peer 恢复和屏幕分享协调器，并补齐架构与竞态测试。
- 完整说明和验证边界见 [v2.9.0 更新公告](./docs/release-notes/v2.9.0.md)。

## 2.8.0 - 2026-08-13

> 这是 2.6.0 之后的下一次正式发布，合并本地 2.6.1、2.7.0 与 2.8.0 三个开发阶段。

### 聊天与房间可靠性

- 文字、图片和链接使用服务器 ACK、同 ID 重试和幂等去重；发送失败会保留原消息和重试入口，
  短暂断线不会让同一条消息重复出现。
- 修复图片复制、最新链接被输入框遮挡、角色说话跑错工位、切房动画残留和悬浮窗状态不同步等问题。
- Windows 屏幕分享只在用户明确选择时请求系统声音，捕获源临时不可用时会重试同一窗口或显示器。

### 录音、昨日房间与 AI 模型

- 录音库补齐前后切换、1 / 1.5 / 2 倍速、收藏筛选、短录音清理和默认 10 GB 容量管理；
  M4A 支持区间读取、拖动播放，并保留原有 Marker。
- 昨日房间补充具体好友、游戏及持续时间，按北京时间正确拆分跨午夜记录。
- 模型管理支持手动首次下载、镜像回退、断点续传、暂停和游戏时降速；界面显示 VibeVoice
  约 1.6 GB、Qwen3.5-4B 约 8.7 GB，以及已下载大小和实时百分比。

### Windows 底层与安全安装

- Electron 升级到 42、React 升级到 19.2、Vite 升级到 8、Tailwind CSS 升级到 4；
  TypeScript 保持生态成熟的 5.9，现有 UI、动画、WebRTC、AEC 与 DeepFilter 完整保留。
- electron-builder / electron-updater 与 GitHub Actions 升级；Renderer 继续通过窄 Preload IPC
  使用系统能力，不能直接访问 Node 或文件系统。
- 新增 `.shanghao-install-manifest`。覆盖更新只清理清单中属于上号的文件；旧版无清单安装也能
  原位升级，不会递归删除整个自定义目录，也不触碰聊天、录音和本地模型。

### 房间代码模块化

- RoomClient 继续作为唯一房间通信入口，内部将可靠聊天、Signaling Bridge、屏幕帧兜底传输和
  Peer 串行操作队列拆成职责明确的模块；原有加入、离开、聊天、屏幕分享与语音 API 保持不变。
- WebRTC Mesh、ICE restart、TURN、音频中继与五人语音实现没有重写，房间通信只是整理代码归属。

### 页面与状态职责

- RoomPage 将屏幕分享面板和麦克风/扬声器设置浮层迁到独立组件，保留原 DOM、样式类名、
  操作路径和动画效果，页面更专注于组合房间场景、聊天和底部控制。
- useRoomState 将聊天记录持久化、成员音量持久化、系统通知和邀请深链监听迁到独立模块，
  房间生命周期仍由同一入口串行控制，避免新增第二份状态来源。

### 动画与长期维护

- 继续使用统一 motion tokens、Framer Motion 和 GSAP context 清理；角色移动仍使用 transform，
  状态切换继续用 operation id 中断旧动画，没有减少入场、离场、走路、说话或弹窗动画。
- 新增架构边界回归测试，防止可靠聊天、屏幕分享和独立页面区域以后重新堆回巨型文件。

## 2.7.0 - 2026-08-13（已合并到 2.8.0，未单独发布）

### 底层现代化

- 桌面运行时升级到仍在官方支持周期内的 Electron 42，React 升级到 19.2，Vite 升级到 8，
  Tailwind CSS 升级到 4；现有房间、动物角色、聊天、设置、录音库、昨日房间和悬浮窗界面保持不变。
- TypeScript 保持在生态成熟支持的 5.9，升级构建、类型、WebSocket、版本判断等基础依赖，
  并移除没有真实代码引用且带来高风险传递依赖的旧 STUN 包。
- 主窗口、悬浮窗和屏幕分享窗口继续使用隔离上下文与沙盒；Renderer 仍不能直接访问 Node、
  文件系统或任意 Electron API，系统能力继续通过窄 Preload IPC 提供。

### Windows 安装与更新

- Windows 打包继续使用 electron-builder、electron-updater 与 NSIS，并升级到当前维护版本；
  自动更新、版本门禁、深链、管理员权限、图标与开机启动流程继续保留。
- 新增 `.shanghao-install-manifest` 文件清单。覆盖升级时只删除清单中明确属于上号的文件，
  目录只在为空时删除，不会递归清理用户自行放入安装目录的内容。
- 兼容没有清单的旧安装版本：先验证 `.shanghao-install-root`，再按固定旧版程序白名单迁移，
  不要求卸载重装，也不触碰 AppData、录音目录或本地 AI 模型。

### 工程维护

- Windows CI 升级到 Node 24、pnpm 10 和新一代官方 Actions；CodeQL 同步升级。
- Dependabot 将 Electron、React 与构建工具分别维护，核心依赖 Major 不再自动混入同一个 PR。
- 保持协议版本 7 和现有生产 `requiredClientVersion` 不变，本地开发阶段不推送、不发布、
  不部署服务器，也不提前拦截仍在使用正式版的好友。

## 2.6.1 - 2026-08-12（已合并到 2.8.0，未单独发布）

### 聊天可靠发送

- 文字、图片和链接预览统一增加 `clientMessageId`、服务端 ACK、结构化失败回执和
  `sending / sent / failed` 状态。消息只有在服务器写入权威历史并开始广播后才算发送成功。
- ACK 超时或短暂断线会使用同一个消息 ID 自动重试；服务器按发送者和消息 ID 幂等去重，
  返回原 ACK，不重复广播或写入。失败消息保留在聊天区，可点击重新发送。
- 客户端在发送前按 256 KB WebSocket 上限检查序列化后的完整 payload；可靠性日志只记录
  消息 ID、房间、类型、大小、耗时、重试和失败原因，不记录正文或图片内容。

### 录音库播放

- `shanghao-recording://` 媒体协议继续在 app ready 前注册为可流式读取的安全标准协议，
  主进程直接处理完整、开放区间和后缀 Range，返回 200 / 206 / 416 及准确媒体头。
- M4A 使用 `audio/mp4`，支持浏览器按需读取、拖动进度和倍速。媒体错误不会自动跳到下一条，
  也不会清空当前录音；界面会说明文件仍保存在本地。

### 版本一致性与昨日房间

- 所有客户端加入路径都检查强制更新状态，signaling join handshake 携带真实版本；服务器可用
  `requiredClientVersion` 最终拒绝旧版、缺失版本或格式错误的客户端。本轮没有修改生产门禁。
- 昨日房间按 `Asia/Shanghai` 拆分跨午夜在线时长，00:00 继承在线成员；身份优先使用
  `profileId`，游戏参与者集合持久化并在服务重启后恢复，v2.6.0 数据原位迁移且保留 14 天。

### 房间视觉、工作图标与更新历史

- 修正动画回调把像素坐标误当场景单位的问题，避免说话或状态刷新后角色错误移动；保留全部
  行走、入场、离场、坐下、空闲、说话、敲门和欢迎动画。
- 以柯基为坐姿体量参考校准猫、熊猫、狐狸与鸭子；鸭子缩小、重新居中并减少椅子侧边露出。
  名字框加宽并重新分配昵称和延迟空间，`grampus` 等常用昵称优先完整显示。
- Codex 优先从官方 `AppxManifest.xml` 的 VisualElements Logo 读取图标，再回退到
  Windows 可执行文件图标；普通 ChatGPT 安装路径不会被识别成 Codex。
- 更新公告使用统一详细 Viewer，分类自动分页，支持本地图片和“怎么用”；恢复仓库可核验的
  59 个标签，并加入无标签正式版本，设置页永久离线保留共 64 条历史，不再限制 10 条。

## 2.6.0 - 2026-08-12

### 录音库与自动保存

- 设置页新增录音库：可更改和打开本地目录，直接播放、拖动进度、查看录音标记、
  切换倍速、播放下一条并删除单条录音。
- “加入频道时自动录音”升级为默认开启的“自动录音并保存”；离开频道时直接写入
  录音库，不再弹出保存确认。录音按房间与时间命名，手动录音仍保留确认流程。
- 录音空间上限默认 10 GB，可自行调整；超过上限后从最旧文件开始清理，并保留最新录音。

### 昨日房间与聊天

- 服务器按一号房、二号房分别汇总最近 14 个完整自然日，记录来过的朋友、房间亮起
  时长、最高在线、消息、屏幕分享、游戏和最后离开时间，不统计说话时长或排行。
- 更新说明优先显示，退出动画完成后才检查昨日房间；安静的日期不会自动打扰，但仍可在
  设置 → 房间记录中查看。每个房间的已读状态独立保存在本机。
- 同一好友短时间连续发言自动分组；文字、图片和链接都支持右键直接复制，并显示统一提示。

### 房间视觉与日常操作

- 房间背景改为更干净的冷白与极浅冰蓝，降低绿色雾感；桌子减少过曝高光，桌面、结构边缘
  和冷灰蓝落地阴影恢复层次，空显示器使用蓝黑色。
- 坐下角色放大并靠近工位，名字框重新均分内容；选中工位更清透，不再让蓝色边线穿过角色。
- 底栏按钮统一悬停反馈与状态色，悬浮窗开启使用蓝色，退出按钮默认降权、悬停时才突出红色。
- Windows 邀请协议会在开发环境启动时修复旧注册参数，避免把调试参数误当成应用目录。
- 修正鸭子椅子偏到左侧的问题；链接预览完成排版后会再次贴住聊天底部，不再被输入框截住。
- Codex 桌面版按实际安装进程识别，切回上号后仍会保留已打开的工作状态图标。

### 服务器兼容

- 保持协议版本 7，新增能力采用兼容字段与服务器构建号检测；旧服务器不会再因为不认识
  “昨日房间”请求而把聊天和快捷消息一起停用。升级固定服务器后即可启用 14 日房间记录。
- 固定服务器尚未升级时，房间记录会明确显示“当前服务器暂不支持”，不再一直停在读取状态。

## 2.5.0 - 2026-08-11

### 房间切换与屏幕分享

- 一号房和二号房切换改为单一串行事务：先取消仍在加载的分享来源、停止本地分享、
  关闭独立观看窗口并断开旧房间，再进入新房间，避免重复占座、角色残留和重复提示。
- 屏幕分享来源列表支持真正取消；过期的窗口枚举结果不会重新打开选择器，失败后可
  直接重试，退出与切房会立即停止所有本地 Track 并清空来源、错误和观看状态。
- 顶部移除容易与角色、悬浮窗重复且抖动的“某某说话中”文字，只保留稳定连接状态。

### 语音、聊天与日常体验

- 保留 2.2.2 起的五人真实 PCM 播放验证、隐藏解码泵和逐成员 Relay 兜底；继续通过
  五个独立 Electron 客户端的 20 条定向音频路径验证。
- 说话检测在误触发后仍会慢速学习持续底噪，风扇声或虚拟设备底噪不会让角色和悬浮窗
  永久保持绿色；真实说话仍保留短暂尾音，房间与悬浮窗共用同一状态。
- 消息增加本人撤回：服务器验证作者身份、从持久聊天历史删除，并把撤回事件广播给
  房间内所有客户端，不会只在本机消失。
- 首页补充自动增益开关，与设置页使用同一份设置即时双向同步；最近版本记录扩展为
  10 条，默认展开最近 4 条，其他版本可逐条查看用户化说明。

### Windows 安装安全

- 安装目录写入固定内容的上号专属标记；清理前必须读取并验证标记，旧版本只有在
  主程序、`resources/app.asar` 和卸载器完整存在时才允许迁移一次标记。
- 删除安装根目录的递归清理，只对白名单 Electron/上号程序文件和明确子目录执行清理；
  自定义目录中的其他用户文件不会被删除，同名程序也不会被跨目录误杀。

## 2.4.0 - 2026-08-08

### 五人语音与弱网恢复

- 保留五人全互联、共享远端混音器和真实 PCM 播放验证；信令语音兜底不再固定预热
  12 秒，只对尚未完成可播放验证或明确请求恢复的成员定向发送。
- 兜底音频增加自适应语音活动门控、120 ms 预卷和 260 ms 尾音保持，静默时不占用
  服务器带宽，单次键盘敲击也不会轻易打开整条语音流。
- 单成员异常优先执行 ICE restart，失败后才重建该成员连接；不会为了一个人的网络问题
  重置整个五人房间。

### Windows 网络与启动可靠性

- 正式安装包明确请求管理员权限；开发模式保持普通权限，避免日常开发和调试被 UAC 干扰。
- 开机启动改为当前用户的最高可用权限计划任务，并保留 5 秒延迟，更新安装路径后会自动修复。
- 新增只属于 `ShangHao Network` 组的四条应用级防火墙规则，覆盖 TCP/UDP 出站与入站；
  设置页可查看和修复，不修改用户已有规则。

### TURN、带宽与日常体验

- 固定服务器可通过受保护的 `/ice-config` 下发短期 TURN HMAC 凭据；支持 UDP、TCP、
  TURNS 5349，以及不与现有 HTTPS 冲突时的可选 TLS 端口。
- 新增安全的 4 Mbps Linux 测试脚本和验收文档；脚本只在人工运行时限速，并保证退出恢复。
- 麦克风发送音量、扬声器总音量、自动入房录音、珍藏未读和悬浮窗状态补齐；屏幕分享
  停止、退出与外链打开统一走幂等清理和安全主进程入口。

## 2.3.0 - 2026-08-02

### 五人语音与会话恢复

- 五个独立 Electron 客户端继续验证全部 20 条成员到成员音频方向，并覆盖第五人
  晚加入、单成员连接销毁和双向恢复；远端成员统一接入一个共享音频混音器。
- 修复旧连接异常残留导致重开软件后出现“幽灵成员”的问题：同一资料的新连接会
  原子替换旧会话、释放旧角色和音频路由，不再挤占五人房间。
- 说话检测改为自适应噪声底与短时保持，降低键盘声误触发和状态文字快速抽搐。

### 设备、屏幕分享与人声

- 进入频道前可直接选择麦克风和扬声器，并分别完成真实采集与播放设备检查；兼容
  RODE Connect 等虚拟音频设备，不再只靠设备标签判断可用性。
- 屏幕分享在枚举窗口前临时关闭内容保护并自动重试，缩略图失败时仍保留可选来源；
  成功取得媒体流后恢复保护，固定使用 1440p 分享质量。
- 设置页移除无操作价值的降噪说明和固定输入阈值，新增默认开启的人声增强；
  DeepFilterNet、低切、自动增益和人声均衡在同一条 48 kHz 本地链路中工作。

### 动画、悬浮窗与频道珍藏

- 角色进入、换座、离开统一为可打断动画事务；快速连续点座位会从当前画面位置
  平滑转向新目标，不再发生坐姿漂移、抽搐或多个动画互相争抢。
- 去除主场景靠近工位的横向装饰线，页面、角色姿态和状态内容统一使用非线性淡入淡出。
- 悬浮窗改为屏幕左侧纵向成员列表，显示头像、昵称以及说话、静音、空闲状态。
- 新增“频道珍藏”，可把短文字、链接和图片地址持久保存在服务器房间数据中，
  并在所有成员之间即时同步。

## 2.2.2 - 2026-08-01

### 五人互听修复

- 修复第四、第五位成员能够发声、却可能收不到既有成员声音的问题：接收端不再只以
  RTP 到达作为播放成功，而是继续验证 Chromium 是否真正解码出可播放 PCM。
- WebRTC 轨道增加隐藏解码泵，统一远端混音器会主动恢复暂停的播放上下文；只有
  音频已真实解码后才关闭对应成员的信令音频兜底。
- 五个独立 Electron 客户端的 20 条定向音频路径全部完成真实波形验证，并覆盖
  第五人晚加入与连接销毁后的双向恢复。

### 动画与昵称

- 角色退出取消起步前的硬停顿，沿离场路线连续行走并在出口附近渐隐；路径重定向
  保留当前速度，减少快速切换时的顿挫。
- 客户端与服务端共享的昵称校验补充数字前后缀、分隔符和常见字形替换，`dad123`、
  `123dad`、`d4d-123` 等写法不能再绕过限制。

## 2.2.1 - 2026-07-31

### 游戏识别扩展

- KK 官方对战平台保持后台运行时不再被误判为正在游戏；只有真实游戏进程、窗口、
  路径或地图参数提供有效证据后才更新成员状态。
- 新增《向魔兽开炮》《尸潮庇护所》、DotA 1、魔兽争霸 3、CS 1.6、红色警戒 2、
  拳皇 97、星际争霸等 KK 常见游戏与地图识别。
- 扩展穿越火线、地下城与勇士、魔兽世界、炉石传说、燕云十六声、鸣潮、绝区零、
  暗区突围：无限、逃离塔科夫、极限竞速：地平线 5 等常见游戏。
- 新游戏尚未配置专属封面时使用稳定的通用游戏图标与名称，不再出现空白显示。

### 质量保障

- 增加 KK 启动器防误报、共享宿主进程地图证据、经典游戏与通用图标回退测试。
- 游戏探测继续使用只读 Windows 进程信息，不注入游戏、不模拟输入、不读取游戏内存。

## 2.2.0 - 2026-07-30

### 五人语音可靠性

- 新增五个独立 Electron 客户端的真实 WebRTC 媒体验收：覆盖 10 条全互联连接、
  20 条定向入站 RTP、第五人晚加入，以及连接销毁后的双向恢复。
- 每位好友的 WebRTC、语音兜底、队列与丢帧状态写入统一诊断；单个成员异常时
  只重建该成员的媒体路径，不重置整个房间。
- 语音兜底继续使用有界队列和按成员路由，避免多人时陈旧音频排队拖垮实时通话。

### 降噪与屏幕分享

- DeepFilterNet 模型在应用空闲时后台预热，房间状态只在处理节点真正工作后显示
  “降噪已开启”；模型异常保持原声发送，不再用重复弹窗打断入场。
- 麦克风处理固定为 48 kHz，移除旧采样率偏好；录音导出仍独立使用 32 kHz。
- 屏幕分享默认只走 WebRTC 直接媒体流，旧截图兜底仅在接收端明确请求时启用，
  不再错误依赖语音轨道是否就绪。

### 维护

- 发布流程加入 `test:five-peer-media`，补齐动效职责边界、运行时音频诊断和回归测试。
- 清理 RNNoise 旧实现与许可残留，DeepFilterNet 保持为唯一降噪引擎。

## 2.1.1 - 2026-07-27

### 房间可靠性

- 固定频道内的五个角色改为服务器原子占用：已被好友选择的角色会在进入页禁用，
  同时阻止并发加入或房间内换角造成重复角色。
- 角色在异常掉线后的短期重连窗口内继续保留，正常退出或重连超时后自动释放。

### 音频与体验

- 新增单成员本地音量面板，可将每位好友单独调至 `0–200%`、仅在本机静音，
  并记住每位好友的播放音量。
- 远端 WebRTC 与 Relay 音频统一使用同一套音量边界，避免切换媒体路径后音量失效。
- 角色行走与座位切换继续收敛为可打断状态，减少快速操作时的漂移与残留状态。

### 安全与维护

- 昵称校验迁移到共享包，客户端和服务端使用同一套规则，补充常见变体和规避写法。
- 增加角色占用、并发抢占、释放、单成员音量和界面回归测试。

## 2.1.0 - 2026-07-26

### 稳定性

- 修复第四、第五位成员加入时可能出现的单向无声：按成员串行化 WebRTC
  协商，并以实际入站 RTP 作为关闭 Relay 兜底的条件。
- 增加短期重连会话令牌，降低断线重连时被同名连接错误替换的风险。
- 将远端 WebRTC 和 Relay 音频统一接入共享混音器，按成员无缝切换媒体路径。

### 功能

- 自动离开仅依据 Windows 系统空闲时间；自动返回和手动返回统一遵循扬声器状态。
- 新增“自动识别正在玩的游戏”总开关，可立即停止检测并清除游戏状态。
- 屏幕分享改由单一 Manager 管理，独立观看窗口使用最小权限 Preload。
- 提示音设置收敛为总开关和总音量，自己与好友的进入、退出音效分离。

### 体验与维护

- 统一 GSAP、Framer Motion 与 CSS 的职责和动效 Token。
- 拆分角色渲染、标签、反应、移动运行时和性格配置，减少场景组件耦合。
- 视觉截图自动化迁出正式主进程并从安装包排除。
- Caddy 部署改为独立 `conf.d` 片段，验证失败自动回滚。
- 删除 macOS 构建入口，明确只发布 Windows 安装包。

## 2.0.0 - 2026-07-23

- 发布第一版完整角色动画、五人工位场景、共享音频混音器和屏幕分享体验。
