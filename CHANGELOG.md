# Changelog

上号只记录正式对外版本。当前正式支持平台为 Windows 10/11 x64。

## 2.4.0 - 2026-08-08

### 五人语音与弱网恢复

- 保留五人全互联、共享远端混音器和真实 PCM 播放验证；信令语音兜底不再固定预热
  12 秒，只对尚未完成可播放验证或明确请求恢复的成员定向发送。
- 兜底音频增加自适应语音活动门控、120 ms 预卷和 260 ms 尾音保持，静默时不占用
  服务器带宽，单次键盘敲击也不会轻易打开整条语音流。
- 单成员异常优先执行 ICE restart，失败后才重建该成员连接；不会为了一个人的网络问题
  重置整个五人房间。

### Windows 网络与启动可靠性

- 正式安装包明确请求管理员权限；开发模式保持普通权限，避免日常开发和调试被 UAC 干扰。
- 开机启动改为当前用户的最高可用权限计划任务，并保留 5 秒延迟，更新安装路径后会自动修复。
- 新增只属于 `ShangHao Network` 组的四条应用级防火墙规则，覆盖 TCP/UDP 出站与入站；
  设置页可查看和修复，不修改用户已有规则。

### TURN、带宽与日常体验

- 固定服务器可通过受保护的 `/ice-config` 下发短期 TURN HMAC 凭据；支持 UDP、TCP、
  TURNS 5349，以及不与现有 HTTPS 冲突时的可选 TLS 端口。
- 新增安全的 4 Mbps Linux 测试脚本和验收文档；脚本只在人工运行时限速，并保证退出恢复。
- 麦克风发送音量、扬声器总音量、自动入房录音、珍藏未读和悬浮窗状态补齐；屏幕分享
  停止、退出与外链打开统一走幂等清理和安全主进程入口。

## 2.3.0 - 2026-08-02

### 五人语音与会话恢复

- 五个独立 Electron 客户端继续验证全部 20 条成员到成员音频方向，并覆盖第五人
  晚加入、单成员连接销毁和双向恢复；远端成员统一接入一个共享音频混音器。
- 修复旧连接异常残留导致重开软件后出现“幽灵成员”的问题：同一资料的新连接会
  原子替换旧会话、释放旧角色和音频路由，不再挤占五人房间。
- 说话检测改为自适应噪声底与短时保持，降低键盘声误触发和状态文字快速抽搐。

### 设备、屏幕分享与人声

- 进入频道前可直接选择麦克风和扬声器，并分别完成真实采集与播放设备检查；兼容
  RODE Connect 等虚拟音频设备，不再只靠设备标签判断可用性。
- 屏幕分享在枚举窗口前临时关闭内容保护并自动重试，缩略图失败时仍保留可选来源；
  成功取得媒体流后恢复保护，固定使用 1440p 分享质量。
- 设置页移除无操作价值的降噪说明和固定输入阈值，新增默认开启的人声增强；
  DeepFilterNet、低切、自动增益和人声均衡在同一条 48 kHz 本地链路中工作。

### 动画、悬浮窗与频道珍藏

- 角色进入、换座、离开统一为可打断动画事务；快速连续点座位会从当前画面位置
  平滑转向新目标，不再发生坐姿漂移、抽搐或多个动画互相争抢。
- 去除主场景靠近工位的横向装饰线，页面、角色姿态和状态内容统一使用非线性淡入淡出。
- 悬浮窗改为屏幕左侧纵向成员列表，显示头像、昵称以及说话、静音、空闲状态。
- 新增“频道珍藏”，可把短文字、链接和图片地址持久保存在服务器房间数据中，
  并在所有成员之间即时同步。

## 2.2.2 - 2026-08-01

### 五人互听修复

- 修复第四、第五位成员能够发声、却可能收不到既有成员声音的问题：接收端不再只以
  RTP 到达作为播放成功，而是继续验证 Chromium 是否真正解码出可播放 PCM。
- WebRTC 轨道增加隐藏解码泵，统一远端混音器会主动恢复暂停的播放上下文；只有
  音频已真实解码后才关闭对应成员的信令音频兜底。
- 五个独立 Electron 客户端的 20 条定向音频路径全部完成真实波形验证，并覆盖
  第五人晚加入与连接销毁后的双向恢复。

### 动画与昵称

- 角色退出取消起步前的硬停顿，沿离场路线连续行走并在出口附近渐隐；路径重定向
  保留当前速度，减少快速切换时的顿挫。
- 客户端与服务端共享的昵称校验补充数字前后缀、分隔符和常见字形替换，`dad123`、
  `123dad`、`d4d-123` 等写法不能再绕过限制。

## 2.2.1 - 2026-07-31

### 游戏识别扩展

- KK 官方对战平台保持后台运行时不再被误判为正在游戏；只有真实游戏进程、窗口、
  路径或地图参数提供有效证据后才更新成员状态。
- 新增《向魔兽开炮》《尸潮庇护所》、DotA 1、魔兽争霸 3、CS 1.6、红色警戒 2、
  拳皇 97、星际争霸等 KK 常见游戏与地图识别。
- 扩展穿越火线、地下城与勇士、魔兽世界、炉石传说、燕云十六声、鸣潮、绝区零、
  暗区突围：无限、逃离塔科夫、极限竞速：地平线 5 等常见游戏。
- 新游戏尚未配置专属封面时使用稳定的通用游戏图标与名称，不再出现空白显示。

### 质量保障

- 增加 KK 启动器防误报、共享宿主进程地图证据、经典游戏与通用图标回退测试。
- 游戏探测继续使用只读 Windows 进程信息，不注入游戏、不模拟输入、不读取游戏内存。

## 2.2.0 - 2026-07-30

### 五人语音可靠性

- 新增五个独立 Electron 客户端的真实 WebRTC 媒体验收：覆盖 10 条全互联连接、
  20 条定向入站 RTP、第五人晚加入，以及连接销毁后的双向恢复。
- 每位好友的 WebRTC、语音兜底、队列与丢帧状态写入统一诊断；单个成员异常时
  只重建该成员的媒体路径，不重置整个房间。
- 语音兜底继续使用有界队列和按成员路由，避免多人时陈旧音频排队拖垮实时通话。

### 降噪与屏幕分享

- DeepFilterNet 模型在应用空闲时后台预热，房间状态只在处理节点真正工作后显示
  “降噪已开启”；模型异常保持原声发送，不再用重复弹窗打断入场。
- 麦克风处理固定为 48 kHz，移除旧采样率偏好；录音导出仍独立使用 32 kHz。
- 屏幕分享默认只走 WebRTC 直接媒体流，旧截图兜底仅在接收端明确请求时启用，
  不再错误依赖语音轨道是否就绪。

### 维护

- 发布流程加入 `test:five-peer-media`，补齐动效职责边界、运行时音频诊断和回归测试。
- 清理 RNNoise 旧实现与许可残留，DeepFilterNet 保持为唯一降噪引擎。

## 2.1.1 - 2026-07-27

### 房间可靠性

- 固定频道内的五个角色改为服务器原子占用：已被好友选择的角色会在进入页禁用，
  同时阻止并发加入或房间内换角造成重复角色。
- 角色在异常掉线后的短期重连窗口内继续保留，正常退出或重连超时后自动释放。

### 音频与体验

- 新增单成员本地音量面板，可将每位好友单独调至 `0–200%`、仅在本机静音，
  并记住每位好友的播放音量。
- 远端 WebRTC 与 Relay 音频统一使用同一套音量边界，避免切换媒体路径后音量失效。
- 角色行走与座位切换继续收敛为可打断状态，减少快速操作时的漂移与残留状态。

### 安全与维护

- 昵称校验迁移到共享包，客户端和服务端使用同一套规则，补充常见变体和规避写法。
- 增加角色占用、并发抢占、释放、单成员音量和界面回归测试。

## 2.1.0 - 2026-07-26

### 稳定性

- 修复第四、第五位成员加入时可能出现的单向无声：按成员串行化 WebRTC
  协商，并以实际入站 RTP 作为关闭 Relay 兜底的条件。
- 增加短期重连会话令牌，降低断线重连时被同名连接错误替换的风险。
- 将远端 WebRTC 和 Relay 音频统一接入共享混音器，按成员无缝切换媒体路径。

### 功能

- 自动离开仅依据 Windows 系统空闲时间；自动返回和手动返回统一遵循扬声器状态。
- 新增“自动识别正在玩的游戏”总开关，可立即停止检测并清除游戏状态。
- 屏幕分享改由单一 Manager 管理，独立观看窗口使用最小权限 Preload。
- 提示音设置收敛为总开关和总音量，自己与好友的进入、退出音效分离。

### 体验与维护

- 统一 GSAP、Framer Motion 与 CSS 的职责和动效 Token。
- 拆分角色渲染、标签、反应、移动运行时和性格配置，减少场景组件耦合。
- 视觉截图自动化迁出正式主进程并从安装包排除。
- Caddy 部署改为独立 `conf.d` 片段，验证失败自动回滚。
- 删除 macOS 构建入口，明确只发布 Windows 安装包。

## 2.0.0 - 2026-07-23

- 发布第一版完整角色动画、五人工位场景、共享音频混音器和屏幕分享体验。
