# v2.6.1 旧文件审计

本轮只审计，不删除。判断依据是引用关系、构建入口与历史用途，不以文件年龄作为删除依据。

## KEEP

- `apps/desktop/src/renderer/src/assets/avatars/**`：五个角色、行走帧、坐姿和动画仍在房间使用。
- `apps/desktop/src/renderer/src/assets/scenes/**`：工位、椅子和更新公告本地图片继续使用。
- `apps/desktop/tests/five-peer-*` 与音频 worklet 测试：保护五人语音和真实媒体路径。
- `apps/desktop/build/installer.nsh`：正式发布阶段的安装目录安全保护，当前不可删除。
- `docs/release-notes/**`、审计与迁移文档：更新历史证据来源。

## ARCHIVE_CANDIDATE

- 早期直连、macOS 和旧连接模式文档：主产品已固定为 Windows 固定服务器，但这些文件仍是
  版本历史证据；如要归档，应单独评审链接与 Release 引用。

## REMOVE_CANDIDATE

- 本轮没有发现可以在不影响功能、动画、角色资源、五人语音验收或版本证据的前提下安全删除的文件。
